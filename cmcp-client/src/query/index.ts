export enum AuthQueyKey {
  Login = "login",
  Logout = "logout"
}

export enum BooksQueryKey {
  BOOKS = "books",
  FIND_BOOK = "find:book",
  UPDATE_BOOK = "update:book",
  CREATE_BOOK = "create:book",
  DELETE_BOOK = "delete:book",
}

export enum AuditQueryKey {
  LOG = "log",
  CSV = "csv",
  LOGS = "logs",
  AUDIT = "logs:audit"
}

export enum GenreQueryKey {
  GENRE = "genre"
}

export enum AuthorsQueryKey {
  AUTHORS = "authors"
}

export enum PublisherQueryKey {
  PUBLISHER = "publisher"
}
